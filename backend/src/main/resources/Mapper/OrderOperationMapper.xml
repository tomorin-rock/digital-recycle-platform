<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.OrderOperationMapper">
    <insert id="insert">
        insert into orderOperationlog(orderType, orderId, operatorId, operateRole, action, detail)
        values (#{orderType}, #{orderId}, #{operatorId}, #{operateRole}, #{action}, #{detail})
    </insert>
    <select id="selectAll" resultType="com.demo.entity.OperationLog">
        select *
        from orderoperationlog
            <where>
                <if test="orderType != null">
                    and orderType = #{orderType}
                </if>
                <if test="operateRole != null">
                    and operateRole = #{operateRole}
                </if>
            </where>
        order by createTime desc
    </select>
</mapper>